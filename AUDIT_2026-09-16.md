# Vector Lake 核心业务流程与实现审计

- 审计日期：2026-09-16
- 审计范围：`vector_lake/` 全部运行时、`scripts/` 守护进程、`tests/` 守护网、`config.json` 与 MCP/CLI 入口
- 方式：只读代码走查 + AST 静态扫描 + 隔离 MEMORY 根实跑探针
- 基线：无版本控制（`git rev-parse` 失败），因此对 9 个被修改文件做了文件级备份（`../vector-lake-baseline-20260916`，含 SHA-256）
- 修复前回归基线：`338 passed, 1 failed`（`test_portability`，由会话开始时的并发编辑引起，非本次缺陷）

本报告只记录**有证据支撑**的结论。每条缺陷都给出可复现路径或实测数字。未验证的推断在末尾单列为“未决”。

---

## 0. 结论摘要

审计共发现 **17 项缺陷**：P0（主流程断路 / 数据丢失 / 隐私）**5 项**，P1（静默降级、不可观测失败）**5 项**，P2（死代码、误导性守卫、卫生）**7 项**。其中 2 项属于**功能整体不可用**（F1 入库流水线、F2 canonical 引导与同步），3 项属于**静默数据丢失或越界删除**（F3 治理队列、F4 悬挂边、F5 级联删除）。

最严重的一条：**`prepare_ingest_batch` 在 Python 语义上必然抛 `UnboundLocalError`，整个入库流水线是死的**；同一处缺陷还顺带让 `config.json` 的 `exclude_paths`（隐私排除：`stocks/`、`garmin/`、`personal-insights/`）被静默忽略。测试全绿是因为**回归守护 `tests/test_static_scope.py` 的判定规则存在盲区**——它把函数内任意位置的 import 视为全函数可见，恰好看不见“绑定前引用”。

另一条同源断路：**`governance_store.create_change_set` 引用未定义名 `existing_change_sets`，必然 `NameError`**，导致 V8 引导迁移、`sync_pages_to_canonical`、`ensure_canonical_store_populated`（`graph`/`trace`/`governance_projection` 的自愈引导）在**最需要它们的时刻**（wiki 有页面、canonical 为空）不可用。

---

## 1. P0 — 主流程断路与数据丢失

### F1 `prepare_ingest_batch` 必然 `UnboundLocalError`，入库流水线整体失效

- 位置：`vector_lake/tool_ingest.py:542`、`:649`（修复前）
- 机制：函数体末尾（第 649 行）存在 `import json`。Python 在编译期因此把 `json` 判定为该函数的**局部名**，第 542 行 `json.load(f)` 在第 649 行执行前必然未绑定。
- 两层后果：
  1. 第 540-544 行的 `try/except Exception: config = {}` **吞掉了 `UnboundLocalError`**，于是 `target_directories` / `exclude_paths` / `supported_extensions` 全部回落到默认值 —— **隐私排除清单被静默忽略**；
  2. 第 626 行 `json.dump(...)` 位于 `except` 之外，直接抛出 → 只要存在待入库文件，调用必然失败。
- 实测（修复前）：
  ```
  File "vector_lake/tool_ingest.py", line 626, in prepare_ingest_batch
      json.dump(currently_processing, f)
  UnboundLocalError: cannot access local variable 'json' where it is not associated with a value
  ```
- 爆炸半径：`sync_vector_lake`（watchdog 的 `RawWatchdogHandler`）、`sync` 技能、MCP `sync_vector_lake`、CLI `sync` —— 全部入口。
- 第二缺陷（同一次调用暴露）：`prepare_ingest_batch` 直接 `get_connection()` 而不先 `init_db()`，全新知识库上抛 `no such table: processed_files`。
- 第三缺陷：`_read_relevant_index_context` 在 `wiki/index.json` 不存在时抛错，导致**冷启动（空 wiki + 有待入库源）无法入库** —— 而 `index.json` 正是入库之后才产生的。

### F2 `create_change_set` 引用未定义名，canonical 引导路径全灭

- 位置：`vector_lake/governance_store.py:1306`（修复前）：`existing_change_sets["items"].append(change_set)`
- 实测（修复前）：
  ```
  File "vector_lake/governance_store.py", line 1306, in create_change_set
      existing_change_sets["items"].append(change_set)
  NameError: name 'existing_change_sets' is not defined. Did you mean: 'pending_change_sets'?
  ```
- 传播链：`ensure_canonical_store_populated()` → `migrate_existing_wiki(dry_run=False)` → `create_change_set(...)`。
  被 `tool_graph.visualize`、`tool_trace.trace`、`governance_projection` 调用。
- `NameError` 发生在 `with transaction():` 内部 → 事务回滚 → 迁移**什么都留不下**（无脏数据，但整条路径不可用）。
- 附带缺陷：`create_change_set` 的 `dry_run` 参数**从未被使用**。调用方传 `dry_run=True` 会得到一次**真实生效并落盘**的变更集 —— 破坏性 API 语义错位。
- 附带缺陷：`sync_pages_to_canonical` 用 `os.path.exists(path)` 判断“页面是否已删除”。传入**裸文件名**（`"Concept_X.md"`）时永远判为“已删除”，于是**删掉 canonical 实体而 Markdown 文件仍在盘上** —— 正是本系统最该避免的 canonical/投影漂移。

### F3 治理队列 read-modify-write 丢失更新（静默数据丢失）

- 位置：`governance_store._save_db_queue` + 所有 `load → append → save` 调用点
- 机制：`_save_db_queue` 以“键差集替换”落盘 —— 会 `DELETE` 调用方快照中不存在的所有键。因此任何未持锁的“读快照 → 追加 → 保存”都会**删除并发写入者在窗口期追加的条目**。
- 实测（修复前，4 线程 × 8 条）：
  ```
  UNLOCKED writers: expected 32, persisted 8, lost 24
  ```
  **75% 的治理条目被静默丢弃。**
- 受影响写入者（部分已加 `governance_queue.lock`，部分没有 → 锁形同虚设）：
  `enqueue_governance_item`、`create_merge_suggestions`、`create_change_set`、`create_change_set_from_content`、`publish_change_sets`、`tool_graph.audit`、`tool_bulk_reconciliation`、`scripts/community_clustering_daemon.py`、`scripts/semantic_dedup_daemon.py`。
  已加锁的：`mcp_server.propose_schema_mutation`、`governance_service.resolve_governance_item`。
- 额外风险：`create_change_set` 的事务内取文件锁，与 `propose_schema_mutation` 的文件锁内取事务构成**锁序反转**，具备死锁条件。

### F4 派生边删除只删一半，claim 图留下永久悬挂边

- 位置：`governance_store._apply_change_sets_batch_unchecked`（修复前）
  ```python
  conn.execute(f"DELETE FROM claim_graph_edges WHERE source_id IN ({placeholders})", affected_page_params)
  ```
- 两处错误叠加：
  1. `claim_graph_edges` 的主键空间是 **claim_id**，而 `affected_page_params` 是 **page_key** → 该 DELETE **永远匹配 0 行**；
  2. 即便改成 page_key，也只删 `source_id` 一侧，另一页指向被删除 claim 的边仍然留存。
- 后果：重写页面（claim 文本变化 → claim_id 变化）后，`claim_graph_edges` 中指向已消失 claim 的边永久悬挂，`build_claim_graph_projection` 之外没有任何清理点。
- 同时：`page_graph_edges` 被 `governance_store` 与 `indexer` **双写**（`replace_page_graph_edges` / `replace_page_graph_edges_for_node`），违反单一所有者；治理侧的单侧删除还会在“变更集已应用、索引尚未更新”的窗口内产生**单边图**。
- 已加回归测试：`tests/test_canonical_change_sets.py::test_change_set_apply_deletes_both_edge_directions`（修复前失败）。

### F5 `delete_source` 级联删除：前缀/子串误匹配 + 无恢复点 + processed_files 泄漏

- 位置：`vector_lake/tool_delete.py`
- 三处缺陷：
  1. `filename.lower().startswith(f"source_{raw_stem.lower()}")` —— 删除 `raw/Annual.md` 时会连带删除 `Source_AnnualReport.md`（`"source_annualreport.md".startswith("source_annual")` 为真）；
  2. `raw_ref_lower in source.lower() or raw_basename_lower in source.lower()` —— 子串匹配，`Annual.md` 命中 `raw/archive/AnnualReport.md`；
  3. 删除 Markdown 页面前**不创建任何恢复点**（对比 `tool_gc` 会先备份），且 `mutation_outbox` 的 delete 行 `payload_text` 为 `NULL` → **wiki 页面内容不可恢复**。
- 附带：删除原始源后不清理 `processed_files` 行 → 同一源重新加入时因 hash 相同被判定“已处理”，**永远不会再入库**。

---

## 2. P1 — 静默降级与不可观测失败

### F6 `finalize_ingest` 把基础设施故障伪装成“载荷被拒绝”

- 位置：`tool_ingest.py`（修复前）：`except Exception as e: return f"Error finalizing ingestion: {e}\n{traceback}"`
- 数据库不可用、outbox 写入失败、健康门拦截等**基础设施故障**与“payload 校验不通过”返回**同一种字符串**；调用方（MCP / 宿主 subagent）无法区分“改载荷重试”与“存储坏了”，且工具调用被上报为成功。

### F7 outbox 唤醒信号写到了没人轮询的路径

- 生产者：`mutation_coordinator._signal_outbox_consumer` → `<extension_root>/tmp/outbox_signal.lock`
- 消费者：`watchdog_app.index_worker_loop` → `%TEMP%/vector_lake_tmp/outbox_signal.lock`
- 两者**永不一致**，信号是死代码。影响限于延迟（消费者 1s 轮询兜底），但属明确的错误接线。

### F8 入库去重状态放在系统临时目录，且失败后锁死源文件

- 位置：`tool_ingest.prepare_ingest_batch` / `finalize_ingest` 使用 `tempfile.gettempdir()/vector_lake_tmp/processing_files.json`
- 三个独立问题：
  1. **跨知识库共享**：机器上所有 Vector Lake 实例共用同一份状态文件（系统临时目录还可能被 OS 清理）；
  2. **以内容 hash 为键**：两个不同文件只要内容相同，第二个被静默丢弃，而返回“fully synced”，源文件永不入库；
  3. **失败不释放**：入队失败（实测：`_build_ingest_instructions` 抛错）后 hash 仍被标记，源文件被阻塞整个 3600s TTL；实测第二次调用返回 `"No new files to ingest. System is fully synced."`。
  4. 清理 `finalize_ingest` 中的释放逻辑用 `except Exception: pass` 吞掉全部异常。

### F9 日记同步是“发射后不管”，失败完全不可见

- 位置：`watchdog_app.DiaryWatchdogHandler`
- `subprocess.Popen([...], stdout=DEVNULL, stderr=DEVNULL)` —— 无退出码、无日志、无任务行、无重试、无状态。日记（核心业务流之一）同步失败时系统表现为“一切正常”。
- 附带：脚本路径硬编码 `~/.gemini/scripts/sync_focus.py`（可被环境变量覆盖，但默认值不具备可移植性）。

### F10 `assemble_context` 用正则反向解析自己的格式化输出

- 位置：`tool_search.assemble_context`（修复前）
  ```python
  for match in re.finditer(r"\*\*(.+?)\*\*.*?\n\s+(.*?)\.\.\.\n", search_results, re.DOTALL):
  ```
- 结构化数据被降级为字符串再用正则捞回来；格式化一变即静默产出空 wiki 上下文，且**不报错**。
- 附带（预算不变量失效）：`purpose`（`render_strategy_directive()`）**完全没有长度上限**，`index_summary` 只用 `index_budget` 截断，实际返回的 `budget_used` **可以超过 `budget_max`**。

---

## 3. P2 — 死代码、误导性守卫与卫生问题

| ID | 位置 | 问题 | 证据 |
|---|---|---|---|
| F11 | `wiki_utils.write_markdown_file` | 边界检查是死代码：`try` 内 `raise SafeWriteError` 被同一 `try` 的 `except Exception: pass` 捕获；且该分支的 `"MEMORY" not in str(path)` 逃生口本身**削弱**边界。真正生效的是函数末尾的 `path.resolve() != expected_path` 检查 | `ruff F811` + 人工走查 |
| F12 | `wiki_utils.py` | `class SafeWriteError` **定义两次**（277 / 349 行），后者覆盖前者 | `ruff F811` |
| F13 | `hasattr/schedule`、`tool_lint._write_fixed_frontmatter` 等 | 多处 `except Exception` 降级为 `log.warning` 后静默丢弃自动修复结果 | `ruff F841` 附带扫描 |
| F14 | `native_llm._task_root` | 每次进程启动创建 `brain/runtime-<pid>-<uuid>/`，**无任何回收**；实测已累积 50+ 个目录（44K，几乎全为空壳），无上界；数量随进程启动次数单调增长 | `find brain -maxdepth 1 -type d -name 'runtime-*' \| wc -l` |
| F15 | 回归守护盲区（元缺陷） | `tests/test_static_scope.py` 的 `_bound_in_scope` 把函数内**任意位置**的 import 记为全函数可见，因此**看不见 F1 这类“绑定前引用”**。守护存在，缺陷照样通过 | 对照实测：同一规则对修复前的 `tool_ingest.py` 返回 `problems == []` |
| F16 | 全仓 | 143 项 `ruff` 提示（未用 import、重复定义、单行多语句、模块级 import 位置） | `ruff check vector_lake scripts tests *.py` |
| F17 | `config.json` + `tests/test_portability.py` | 会话开始时二者正被并发编辑，`memory_dir` 的机器相关路径与“禁止机器路径”规则冲突；会话结束时该测试已由那次编辑自行修好 | 文件 mtime 22:46/22:47 vs 本会话首次运行 |

---

## 4. 已核查但**判定为非缺陷**的项（避免误报）

| 观察 | 结论 |
|---|---|
| `enqueue_mutation` 在事务中嵌套调用 `transaction()` | 设计正确：`_LOCAL.in_transaction` 实现嵌套复用 |
| `PRAGMA foreign_keys` 在连接回收后失效 | **当前无实际影响**：全库未声明任何 `FOREIGN KEY` / `REFERENCES`，该 pragma 目前是 no-op。属**潜伏缺陷**（一旦引入外键即静默失效），已修 |
| `tool_search._safe_eval` 过滤器 | 逐节点白名单校验，无属性/下标逃逸路径，`ast.Attribute` 仅允许在 `.get()` 调用内。安全 |
| `scripts/semantic_dedup_daemon.py` 直接调用嵌入 API | **不成立**：它从 SQLite `vec_embeddings` 读向量，并显式声明 `"Semantic dedup never calls the provider directly"`；嵌入受 `embedding_scheduler` 的 RPM/TPM 约束 |
| `compute_debt_metrics` 在索引构建路径中可能写 canonical | 仅当记忆库为空且存在 claim 时触发 `rebuild_operational_memory()`，属自愈引导；调用点不在事务内，无死锁 |
| PPR 注释与实现不一致 | 注释声称“非种子节点也获得传送质量”，实现只向种子集传送。数学上后者是合法的 personalized PageRank，**注释错误而非代码错误** |

---

## 5. 未决与残留风险

1. **锁与崩溃原子的边界**：`governance_queue_session()` 用文件锁串行化跨进程写入，但**锁不提供崩溃原子性**。若进程在 `os.replace` 前崩溃，状态文件保持旧值（安全方向）。`governance_queue` 本身仍以“快照替换”落盘，未改为行级增量写入；这是设计债，未在本次修改（改动面过大且需要迁移）。
2. **`_INDEX_CACHE` 无锁**：`tool_search` 的模块级索引缓存被多线程并发覆盖。由于 `mtime` 与 `data` 成对写入，最坏结果是一次多余的重新加载，自愈。未修改。
3. **`wiki_utils` 的 `MEMORY` 逃生口已删除**，边界依赖 `validate_wiki_filename` + `expected_path` 精确相等。若未来出现合法落盘路径在 `wiki/` 之外，会被拒写 —— 这是刻意的 Clean Cut，但需要使用者知悉。
4. **`archive/` 目录的附带改动**：仓库级 `ruff --fix` 顺带移除了 `archive/*.py` 中的未用 import。该目录无任何导入方（实测 `grep` + `compileall`），改动语义惰性，但属超出原始 write-set 的改动，如实披露。
5. **未验证项**：本次未启动 watchdog 守护进程做长稳运行，因此 `scheduled_lint_loop`、`RawWatchdogHandler` 的真实文件事件路径仅做了静态审查与函数级实跑，未做端到端文件事件验证。
6. **未复现项**：F13 中 `tool_lint._write_fixed_frontmatter` 的静默丢弃在本次审计中未构造出真实触发用例（需构造一个“修复结果仍不合法”的页面），属代码走查结论。
