# Vector Lake architecture review — 2026-09-17

- Scope: `vector_lake/` runtime (49 modules), `scripts/`, MCP/CLI surfaces, and the live
  `C:/Users/shich/MEMORY` state (wiki, `.meta`, SQLite).
- Method: read-only code walk, AST import-graph/SCC analysis, live-DB probes, full test
  suite, hot-path benchmark. **No file in the repository was modified by this review.**
- Baseline measurements taken in this session (`tmp/bench/bench_stage0-baseline.json`,
  `tmp/audit2/pytest_baseline.txt`).

## 0. Executive summary

Vector Lake's read path is in good shape: 450 tests pass in 24.4 s and every operator-facing
read surface is sub-second on the live corpus. The remaining risk is concentrated in three
places, in this order:

1. **Concurrency** — the SQLite write-lock retry has no deadline and can park a call for
   ~31 minutes while the process looks hung.
2. **Durability** — no write path calls `fsync` before `os.replace`, so a crash can leave a
   truncated or zero-length canonical file.
3. **Lost subsystem** — the working tree is a *subset* of the system that produced the
   persisted state. 34 modules that existed on 2026-09-07 are gone, and the artifacts they
   wrote are still on disk with no producer and no consumer.

Item 3 has no clean fix and is the largest architectural risk in the project.

---

## 1. Architecture map

### 1.1 Layers

| Layer | Modules | Contract |
|---|---|---|
| Base | `wiki_utils`, `yaml_utils`, `db_store`, `purpose_contract`, `runtime_health`, `defense_hook` | paths, atomic file writes, SQLite, schema/purpose validation |
| Storage/domain | `governance_store`, `governance_metrics`, `governance_service`, `memory_gram_index`, `page_index_projection`, `embedding_scheduler`, `mutation_coordinator` | canonical rows (claims/evidence/entities), projections, outbox, rate limits |
| Tools | `tool_*` (30 modules), `indexer`, `ingest_worker` | one operator-facing verb each |
| Surfaces | `cli_app` (21 commands), `mcp_server` (39 tools), `tools` (facade), `watchdog_app` | entry points |

Import graph: 49 modules, 123 internal edges, **1 cyclic SCC of 7 modules**
(`db_store, defense_hook, mutation_coordinator, purpose_contract, runtime_health,
tool_timeline, wiki_utils`), down from 33/13/8 in the recent refactor history. Fan-in is
concentrated in `wiki_utils` (28) and `db_store` (15).

### 1.2 Data flow

```
raw/  ──watchdog──► ingest ──► claims + evidence (+ _versions) ──► projections ──► wiki/*.md
                                   │                                  ├─ operational_memory(_index, _gram)
                                   │                                  ├─ timeline_events
                                   │                                  ├─ page_graph_edges
                                   └─ governance_queue                └─ index.json / wiki_search_index
mutation_outbox ──outbox worker──► canonicalise manual edits ──► same projections
```

Two writers exist per surface: the ingest pipeline and the manual-edit outbox. Both end in
`governance_store`, which is why the outbox is lease-fenced and generation-checked.

### 1.3 Live state

| Item | Value |
|---|---|
| wiki pages | 8 488 |
| claims / evidence | 120 339 / 139 339 |
| operational memory | 142 117 rows (+142 117 projected) |
| timeline events | 9 286 |
| `index.json` | 18.8 MB (7 178 nodes, 30 140 weighted edges, 24 324 aliases) |
| `vector_lake.db` | 4.08 GB (995 001 pages × 4 KiB), `auto_vacuum=0`, WAL |
| backups | 7.1 GB in 2 entries |
| journal / pragmas | `WAL`, `synchronous=NORMAL`, `foreign_keys=ON`, `wal_autocheckpoint=1000` |

---

## 2. Measured baseline

`VECTOR_LAKE_MEMORY_DIR=C:/Users/shich/MEMORY python benchmarks/bench_hot_paths.py
--label stage0-baseline --no-embedding --repeat 3`

| Path | p50 (s) | min | max |
|---|---|---|---|
| `operational_memory_search` | 0.470 | 0.455 | 0.472 |
| `build_memory_packet` | 0.985 | 0.951 | 1.002 |
| `assemble_context` | 1.068 | 1.024 | 1.110 |
| `search_vector_lake[page]` | 0.088 | 0.070 | 0.124 |
| `search_vector_lake[memory]` | 0.465 | 0.462 | 0.470 |
| `search_timeline_events` | 0.062 | 0.053 | 0.066 |

Tests: `450 passed, 2 warnings in 24.40s`. Ruff: 86 findings, **all** style
(`E701` ×69, `E402` ×17); no `F`-class (undefined name / unused import) findings.

---

## 3. Findings

Severity: P0 = correctness/availability risk now, P1 = real defect with a bounded trigger,
P2 = resource/efficiency, P3 = hygiene.

### F1 — P0 — the write-lock wait has no deadline (reliability)

`vector_lake/db_store.py:82-108`

```python
conn = sqlite3.connect(str(db_path), timeout=30.0, check_same_thread=False)  # line 60
...
max_retries = 60
for attempt in range(max_retries):
    try: conn.execute("BEGIN IMMEDIATE"); break
    except sqlite3.OperationalError as e:
        if "database is locked" in str(e) and attempt < max_retries - 1:
            time.sleep(0.5 + random.random()); continue
        raise
```

`timeout=30.0` is the SQLite busy timeout, so **each** attempt already blocks inside SQLite
for up to 30 s before raising. The loop then retries 60 times with a 0.5–1.5 s sleep:

    worst case ≈ 60 × (30 s + 1 s) ≈ 31 minutes inside one `with transaction():`

Consequences beyond latency:

- Nothing writes `.watchdog_status.json` during the wait, so `assess_runtime_health`
  reports `watchdog_stale` — the operator sees "watchdog died" rather than "waiting on lock".
- Every writer inherits it: `enqueue_mutation`, `claim_mutation_outbox`, `tool_ingest`,
  `tool_delete`, `tool_gc`, `tool_timeline`, `indexer`, `embedding_scheduler`.
- The retry loop also does not cover `conn.commit()`; if the commit fails the exception
  escapes without a rollback, leaving the connection inside an open transaction, and the
  *next* `BEGIN IMMEDIATE` on that connection raises `cannot start a transaction within a
  transaction` — a misleading error for what is really a stuck lock.

Fix: monotonic deadline + jittered backoff + `PRAGMA busy_timeout` scaled to the remaining
budget + a typed `DatabaseLockTimeout`; roll the connection back when `commit()` fails.

### F2 — P1 — canonical writes are not `fsync`ed before `os.replace` (durability)

`grep -rn fsync vector_lake/` → 0 hits, across five temp-file-then-rename sites:

| Site | Payload |
|---|---|
| `wiki_utils.atomic_write_text` (`:327-331`) | every wiki markdown page |
| `indexer._write_json_payload` (`:250-266`) | `index.json` |
| `indexer._replace_with_retry` (`:1219-1232`) | `index.json`, `claim_graph.json` |
| `native_llm.py:106` | task packets |
| `tool_ingest.py:600` | processing file registry |

`os.replace` is atomic with respect to *readers*, and the code correctly retries
`PermissionError` on Windows. But without `flush()` + `fsync()` the new inode's data may not
be durable when the directory entry is swapped, so a power loss can leave a **zero-length or
torn** `index.json` or wiki page while the previous content is already unlinked.

The markdown wiki is the human-visible canonical layer of this system; `SafeWriteError`
already guards *logical* loss (compiled-truth / evidence-timeline item counts) but nothing
guards *physical* torn writes.

Fix: `handle.flush(); os.fsync(handle.fileno())` before `os.replace`, and fsync the parent
directory on POSIX. Cost is one syscall per file; measurable only on bulk ingest.

### F3 — P1 — the idempotency unique indexes do not exist on the live database (integrity)

`db_store.py:520-534` (mutation_outbox) and `:686-703` (jobs) both swallow
`sqlite3.IntegrityError` and log "Skipping …". The live DB satisfies the trigger condition:

```
idx_mutation_outbox_idempotency  -> SKIPPED
duplicate idempotency_key groups -> 6
  3aa404ec…b4821  ids 26053,26046  completed,superseded
  555dc4ae…b83e   ids 17357,13142  completed,completed
  6514948d…b6a    ids 11397,2803   completed,completed
  7377dd8d…99e    ids 17361,17348  completed,completed
  7b305c67…f38    ids 15136,558    completed,completed
  83ec1d26…8bc    ids 11445,2806   completed,completed
jobs duplicates -> none
```

So on the live database there is currently **no unique index at all** on either table;
`enqueue_mutation` (`db_store.py:1007-1019`) is protected only by the `BEGIN IMMEDIATE`
write lock covering its `SELECT`-then-`INSERT`.

The skip is a deliberate availability trade, and it is the right *default*. What is missing
is the second half: the guarantee can be restored **without deleting audit history** by
falling back to a partial unique index over in-flight rows only —

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_mutation_outbox_idempotency_active
ON mutation_outbox(idempotency_key)
WHERE idempotency_key IS NOT NULL AND status IN ('pending','processing')
```

which is exactly the population the concurrent-enqueue race can produce. All 6 duplicate
groups are terminal, so this index would be created successfully on the live DB today.

### F4 — P1 — backups are unbounded and doubling (operational)

`MEMORY/wiki/.meta/backups` holds 2 entries totalling **7.1 GB**:

```
3.6G  index_rebuild_20260916T144956555311Z
3.6G  vector_lake_1789575103.db.bak
```

`storage_growth.json` recorded `backup_bytes` going **3.90 GB → 7.80 GB in one day**
(2026-09-14 → 2026-09-16). On a 4.08 GB database, each full `backup_database()` doubles
storage. The 2026-09-07 baseline contained `backup_capacity.py` and
`tool_backup_retention.py`; neither exists now, so the retention gate is simply gone.

### F5 — P2 — 389 MB of reclaimable freelist

```
page_size=4096  page_count=995001  ->  4.08 GB
freelist_count=94881               ->  388.6 MB reclaimable
auto_vacuum=0
```

Freelist pages are the residue of prunes (change sets, GC); `auto_vacuum=0` means the file
never shrinks on its own.

### F6 — P2 — long-CJK query cost

`governance_store._query_terms` (`:769-776`) expands a query into every CJK character **and**
every adjacent bigram, so a 30-character question becomes ~59 terms; the SQL scorer then
evaluates 3 `instr()` calls per term per projected row. `CHANGELOG.md:64` records 2.0–2.5 s
for that case (down from 6.97 s on the pre-projection path).

*Status: already being addressed by a concurrent session.* `vector_lake/memory_gram_index.py`
(653 lines: base table + overlay + dirty queue + retire/compact lifecycle + `accumulate_relevance`)
and `tests/test_memory_gram_index.py` appeared during this review window and are being wired
into `governance_store.py:1154-1183`, `cli_app`, `mcp_server`, `tools`,
`page_index_projection` and `indexer`. The live DB already carries
`operational_memory_gram`, `operational_memory_gram_overlay`, `operational_memory_gram_dirty`,
`operational_memory_gram_state` and the `trg_om_gram_dirty_*` trigger set. This review did
not touch it.

### F7 — P0 (architectural) — the tree is a subset of the system that produced its own state

The 2026-09-07 audit baseline (`~/.pi/agent/scratch/vector-lake-audit-20260907-0646/baseline-source/vector_lake/`)
contains 34 modules that do not exist in the current tree:

```
auto_ingest_worker      backup_capacity         cancellation            claim_assessment
decision_registry       diagnostic_snapshot     durability              evidence_foundation
heavy_task_gate         index_snapshot          memory_protocol         merge_analysis
projection_format_v2    projection_store_v2     quality_registry        raw_revision
raw_scrub_contract      restore_snapshot        retrieval_benchmark     runtime_paths
search_projection_contract  storage_growth      tokenizer_runtime       tool_auto_ingest
tool_backup_retention   tool_claim_provenance   tool_evidence           tool_governance_maintenance
tool_legacy_graph_audit tool_registry           tool_semantic_campaign  tool_storage_baseline
tool_wiki_batch         topology_worker
```

Module absence alone is not proof of damage, so here is the *behavioral* evidence that the
loss is live and operator-visible:

1. `MEMORY/wiki/claim_graph.json` is 133 bytes and contains only a locator written by the
   dropped v2 projection stack:
   ```json
   {"contract":"vector-lake-projection-locator","format_version":2,
    "projection":"claim_graph","sidecar":"projection_pair_manifest.json"}
   ```
   Its sidecar `projection_pair_manifest.json` (597 B, 2026-09-16 09:30) is also present.
   **No surviving module reads or writes either file** — `grep` finds only
   `tool_projection.py:192` naming `claim_graph.json` inside a status string.
2. `.meta/storage_growth.json` (contract `vector-lake-storage-growth-v1`) stopped being
   produced because `storage_growth.py` is gone; the daily backup/storage sampler no longer
   runs, which is why F4 went unnoticed until now.
3. The last sample records `projection_object_bytes` 6.40 GB against
   `projection_reachable_bytes` 99.7 MB — i.e. **6.3 GB of orphaned projection objects**
   that only `backup_capacity.projection_v2_reachable_inventory()` (also gone) could
   classify, and nothing can now garbage-collect.

This is the project's largest architectural risk: persisted contracts whose producer *and*
consumer were both lost, invisible from inside the current tree. It is not fixable by
refactoring — it needs a decision about which external state is still authoritative.

### F8 — P3 — scheduler can silently skip a day of maintenance

`watchdog_app.scheduled_lint_loop` (`:440-486`) fires only when
`tm_hour in (10, 23) and tm_min == 0`, sampled every 30 s. If the loop is inside its own
60 s `time.sleep` at that instant, or the process was restarted, the daily lint **and** the
`PRAGMA wal_checkpoint(TRUNCATE)` that is the only periodic WAL truncation are skipped for
the day. There is no catch-up predicate on last-success timestamp.

### F9 — P3 — style / dead indirection

86 ruff findings, all `E701`/`E402`. One is a genuine readability defect:
`wiki_utils.py:421` places `from typing import TypedDict, List` mid-module, after executable
code. `defense_hook.py` (21 lines) is a single-symbol pass-through over `schema_validator`
while participating in the 7-module SCC.

---

## 4. Staged remediation plan

Ordered by (value × confidence) / risk. Stages 1–3 are independent of the concurrent
gram-index work; Stage 4 is not.

| Stage | Change | Files | Verification |
|---|---|---|---|
| **S1** | Bound the lock wait: monotonic deadline (~90 s), jittered exponential backoff, `PRAGMA busy_timeout` set to `min(remaining, 30 s)` per attempt, `DatabaseLockTimeout`, rollback on failed `commit()`. Delete the duplicated `_INIT_LOCK` / unused `_INIT_DB_DONE` (`db_store.py:110-111`). | `db_store.py` | new unit test: a second connection holding `BEGIN IMMEDIATE` must make the first raise `DatabaseLockTimeout` inside the budget, not hang; existing 450-test suite |
| **S2** | `flush()` + `os.fsync(fileno())` before every `os.replace`; directory fsync on POSIX. | `wiki_utils.py`, `indexer.py`, `native_llm.py`, `tool_ingest.py` | injected-`fsync` test asserting ordering; re-run bulk-ingest timing to bound the cost |
| **S3** | Partial unique index fallback over in-flight rows for both `mutation_outbox` and `jobs`; report the active/full/absent state in `doctor`; add a dedup repair that is dry-run by default. | `db_store.py`, `tool_doctor.py` | synthetic DB seeded with duplicate terminal groups → index created, `enqueue_mutation` race still returns one row; live DB re-probe |
| **S4** | *(owned by the concurrent session)* finish wiring `memory_gram_index` into `_indexed_memory_candidates`, keep the legacy path as the differential oracle. | `governance_store.py` | `tests/test_memory_gram_index.py` + `bench_hot_paths.py` before/after |
| **S5** | Backup retention + capacity gate (restore the `tool_backup_retention` / `backup_capacity` behaviour): keep N newest with a byte cap, dry-run default. Then, separately authorised: `PRAGMA incremental_vacuum` / rewrite to reclaim the 389 MB freelist. | new module + `tool_doctor.py` | dry-run listing on the live backups dir; unit test on a synthetic tree |
| **S6** | Scheduler catch-up: persist last successful lint date, run on the first tick whose hour ≥ scheduled hour and date ≠ last run. | `watchdog_app.py` | unit test with injected clock |
| **S7** | Ruff cleanup (mechanical `E701`/`E402`, move the mid-module import), fold `defense_hook` into `schema_validator`. Optional. | several | `ruff check` clean; suite green |

**Not planned without an explicit decision:** anything touching the F7 orphaned projections,
`claim_graph.json`, `projection_pair_manifest.json`, or the 6.3 GB projection object store.

---

## 5. Why stages S1–S3 were not applied in this session

While staging S1, the repository was observed to be under **concurrent write by two other
live Pi sessions**. Evidence:

```
vector_lake/db_store.py            2026-09-17 00:53:37
vector_lake/tool_search.py         2026-09-17 00:53:55
vector_lake/indexer.py             2026-09-17 00:54:15
vector_lake/page_index_projection.py 2026-09-17 00:55:05   <- changed during a 45 s watch window
~/.pi/agent/sessions/…/2026-09-16T15-00-04…jsonl  2026-09-17 00:55:07
~/.pi/agent/sessions/…/2026-09-16T16-11-21…jsonl  2026-09-17 00:54:20
```

Both are mid-implementation of the `memory_gram_index` feature. Their uncommitted write-set
overlaps every file S1–S4 would touch:

```
 M vector_lake/db_store.py            M vector_lake/governance_store.py
 M vector_lake/tool_search.py         M vector_lake/indexer.py
 M vector_lake/cli_app.py             M vector_lake/mcp_server.py
 M vector_lake/tools.py               M vector_lake/page_index_projection.py
?? vector_lake/memory_gram_index.py   ?? tests/test_memory_gram_index.py
```

Their changes are uncommitted, so there is no commit to fork from that would produce a
mergeable patch — a worktree would branch off stale `HEAD` (`ec2d4dc`) and conflict on merge.

Per the execution contract's single-writer rule for a shared resource, mutating these files
now would corrupt in-flight work and make their test runs unreliable. **This review therefore
delivers the analysis, the measured baseline and the staged plan, and stops before the first
write.**

### Minimal decision request

1. **Pause the two other sessions on this repository** — then S1–S3 can be applied directly
   on the working tree in ~3 stages with the verification listed above.
2. Or **I fork an isolated worktree from `HEAD`** and deliver S1–S3 as a pending-proposal
   diff against `ec2d4dc` for later merge, accepting that the merge will need manual
   reconciliation with the gram-index work.
3. Or **restrict me to a non-overlapping write-set**: `wiki_utils.py`, `native_llm.py`,
   `tool_ingest.py`, `tool_doctor.py`, `watchdog_app.py` are untouched by the other writers,
   so S2 (partially), S6 and S7 could proceed immediately without collision.

---

## 6. Residual risk register

| ID | Risk | Severity | Owner decision needed |
|---|---|---|---|
| R1 | 6.3 GB orphaned projection objects with no surviving GC path | high | yes — is that state still authoritative? |
| R2 | `claim_graph.json` + `projection_pair_manifest.json` are orphaned locators | medium | yes |
| R3 | 34 lost modules vs the 2026-09-07 baseline; unknown further orphaned contracts | high | yes |
| R4 | Backups unbounded at 7.1 GB, doubling per full backup | high | no — S5 is mechanical |
| R5 | 389 MB reclaimable freelist, `auto_vacuum=0` | low | no |
| R6 | Write-lock wait can reach ~31 min (F1) | high | no — S1 is mechanical |
| R7 | Torn canonical writes on crash (F2) | high | no — S2 is mechanical |
| R8 | 6 duplicate outbox idempotency groups, no unique index live (F3) | medium | no — S3 is mechanical |
| R9 | Daily lint + WAL truncate can be skipped entirely (F8) | medium | no — S6 is mechanical |

Unverified in this review: no end-to-end watchdog file-event run was performed, and no crash
test was executed (F2 is a code-walk conclusion supported by the absence of `fsync`, not by
a reproduced torn write).
