# PROPOSAL 2026-09-18 — 两项跨批治理提案

本文只提两件事：**D5（`json_extract` 状态模式）** 与 **O 系列（审计判定的过度设计）**。所有数字都是
2026-09-18 在活库（`C:/Users/shich/MEMORY`，7923 个 wiki 页面、DB 2.29 GB、`operational_memory` 146,679 行）
上只读测得的；凡来自 9-16/9-17 审计而未重新测量的，明确标注「审计结论，未复测」。

两项都**不能**塞进一个小批次，原因不是工作量，而是 `pai/coding.md` §1 的反混合要求：目标领域存在两种模式时
必须**整体选定基线**，不能在一个补丁里各改一半。

---

## 一、D5：`json_extract` 状态模式

### 现状（今日实测）

| 量 | 值 |
|---|---|
| 含 `json_extract` 的模块 | 7 |
| `json_extract` 出现次数 | 55 |
| 承载 `data_json` 的表 | 6：`claims`、`evidence`、`sources`、`change_sets`、`governance_queue`、`operational_memory` |
| 建在 `json_extract` 表达式上的索引 | `entities(type/status/page_key)`、`claims(locator.page_key)` |
| 每文件次数 | `db_store` 26、`governance_store` 12、`tool_timeline` 8、`tool_projection` 4、`tool_doctor` 2、`runtime_health` 2、`indexer` 1 |

含义：**同一个「状态」概念有两种落地方式**——一部分字段被 `data_json` 包着、靠 `json_extract` 在查询里现算；
另一部分是同表的独立列。`operational_memory` 已经把这条路走完了一半：`operational_memory_index` 由触发器
（`db_store.py:288-295`）把 `data_json` 里的 8 个字段物化成真列，并在 `source_updated_at` 上比对判断漂移。
也就是说，**代码库里已经存在正确的做法，只是只在一个表上做了**。

### 代价（可验证，不是口味问题）

- 过滤 `$.status`/`$.memory_type` 这类查询无法走索引，只能扫；`db_store.py:582` 的漂移比对就是这种形状。
- 表达式索引是**按表达式**建索引，换一个查询写法（大小写、字段路径）就用不上，于是每加一处过滤都要记得补一条
  索引——这是 55 处调用的实际维护成本。
- `data_json` 里的字段没有类型约束，只有 `COALESCE(..., 'fact')` 这类默认值在兜底。

### 冻结的基线（2026-09-18 实测确认，不再更改）

**模板 = 虚拟生成列 + 普通索引**，而不是再做一张投影表。理由：`operational_memory_index` 那套
（触发器 + `source_rowid` + `source_updated_at` 漂移比对）存在的意义在于**它是另一张表**；若只需
「把 json 里的字段变成可索引的真列」，SQLite 的生成列按构造就与 `data_json` 永远一致，**根本不存在漂移**
—— 于是触发器、漂移比对、`source_rowid` 三者全部不需要。

在本机 sqlite 3.50.4 上逐条实测（临时库，无活数据）：

```
ALTER TABLE claims ADD COLUMN f_claim_type TEXT
    GENERATED ALWAYS AS (json_extract(data_json, '$.claim_type')) VIRTUAL;   -- 允许，嵌套路径 $.locator.page_key 同样可用
CREATE INDEX idx_claims_f_claim_type ON claims(f_claim_type);                -- 允许
EXPLAIN QUERY PLAN ... WHERE f_claim_type = 'fact'
    -> SEARCH claims USING INDEX idx_claims_f_claim_type (f_claim_type=?)    -- 计划确实用它
ALTER TABLE claims ADD COLUMN f_status ... STORED;                           -- 被拒（ALTER 只支持 VIRTUAL）
ALTER TABLE claims DROP COLUMN f_claim_type;                                 -- 允许 → 回滚只是 DDL，无需还原备份
PRAGMA table_xinfo(claims)                                                   -- 检测是否已存在，用于幂等
```

**命名**：`f_<json 路径末段>`（`f_status`、`f_page_key`、`f_claim_type`）。必须与**已有的真列**去重：
`operational_memory` 已有 `status`/`memory_type` 真列，所以那一表的生成列要么不加、要么改名，并**优先改用已有真列**
（那正是当前 json 索引与真列索引并存的原因）。

**基线规则（一句话，用于判定「是否混合」）**：**任何在 WHERE / ORDER BY / JOIN 里使用的 `json_extract(data_json, ...)`
都必须换成生成列；`data_json` 继续作为完整载荷保留；`SELECT` 里纯取值、不参与过滤的 `json_extract`
可保持不动**（它没有索引收益）。按此规则，逐表推进即是**同一模式的部分覆盖**，不构成混合。

**第一张表（`entities`，2026-09-18 已落地）暴露的四条补充规则，后续表必须照做**：

1. **先分类，再决定是否加列**。每个被查询的 json 路径先归为三类：**已是真列**（改查询即可，如本表的
   `type`/`status`：每行都填且与 json 逐行相同）、**只有 json**（加生成列，如 `page_key`）、**死索引**
   （没有任何查询使用 → 删，而不是再加一个拼写）。已知下一个待办：`operational_memory` 的
   `idx_memory_type`/`idx_memory_status` 索引的是 **json**，而全树查询用的是**真列** `memory_type`/`status`
   —— 那两个索引与 entities 的那两个同样死，而 146k 行的表上代价更大；若给它们加生成列，就会在同一张表上
   造出**第三种拼写**。相反，`idx_memory_key(memory_type, json($.memory_key))` 与 `idx_memory_source_claim`
   有真实使用方，必须保留。
2. **收敛前置条件**：任何命名新列的查询，其调用路径必须先 `init_db()`。本批漏掉这条导致写入前健康门
   （`assess_runtime_health` 只在**文件不存在**时 init）在库已存在但缺列时报 `no such column` —— 门先失败，
   而修 schema 的代码在它后面。已修四处（健康门、`tool_projection._canonical_keys`、`indexer` 的两条路径），
   `doctor` 保持只读、改为报「schema 未收敛」而不是抛 SQL 错。
3. **台账只跳过已记录的名字**：若改动要加进一个已执行的迁移，必须用**新名字**（本表的
   `idx_entities_type_status` 因此单独立了一条迁移），否则在真正需要它的库上永远不会执行。
4. **不能退役仍需服务的表达式索引**：`claims` 的 `$.locator.page_key`/`$.claim_type` 与 `evidence` 的
   `$.locator.page_key` 仍有查询按表达式原文使用，改列时必须同时改所有使用方，否则索引白建。

**第二张表（`operational_memory`）又补了六条**：

5. **计数必须说明是在哪张表上**。本批我写「有 4 处 `memory_type IN (...)` 查询」来论证保留真列索引，而复核指出
   那 4 处的 FROM 是 `operational_memory_index` —— 结论不变但理由是错的。凡「N 处」的论证，必须为每一处标出
   `FROM <table>`。
6. **覆盖不等于可达**。「没人用」要同时 grep **列名**与**表达式原文**（含触发器）；「不可达」是结构判断
   （表达式索引无法服务裸列谓词）。
7. **prune 必须以其替代物为前提**：替代索引的创建若位于会吞异常的 `except` 里、且快速路径不再重建，就只能在
   **替代物存在时**才删旧索引；否则不可恢复地退化为全表扫描。相应地，退役一**列**时还必须把它从宽容的
   `ALTER ... ADD COLUMN` 循环里删掉（否则它会被重新加回来）。
8. **非谓词路径不要转**：`tool_timeline` 用 `LIKE`/`COALESCE` 读 `$.subject_entity_ids`/`$.sentiment`/`$.action`，
   任何索引都服务不了，加生成列没有收益 —— 不要为了「一致」而转。
9. **不要把投影表卷进来**：`operational_memory_index`（触发器维护、小写化 blob、`source_updated_at` 漂移比对）是
   另一个消费者，保持独立；否则下一批会试图把检索路径改基到 `f_text`/`f_page_blob` 上。
10. **下一个待办**：`claims` 仍是真列**与** json 拼写并存（`status` 已是真列 → 属于第 1 条里的「改查询即可」），
    而 `idx_claims_page_key`/`idx_evidence_page_key`/`idx_claims_claim_type` 正被 `tool_timeline` 等按**表达式原文**
    使用 → 换列必须在同一提交里改掉全部使用方（第 4 条）。

**仍未决**：本表的 `ttl`/`decay_weight` 是残留列（7,924 行里 7,919 行为 NULL，写入路径还不一致：单条 upsert 写
`0.0`、批量路径不写）。要为后续五张表定模板，须在此明确：**删掉这两列**，还是记录为何保留。

**逐表流程（每表一个提交，模板完全相同）**：

1. 冻结基线：记录该表行数与每个待迁移 json 键的非空计数（`SELECT COUNT(*) WHERE json_extract(...) IS NOT NULL`），
   以及现有一批查询的**结果集快照**（id 列表）。
2. 加生成列 + 建索引（`table_xinfo` 探测幂等），**不改任何查询**；此时 `EXPLAIN` 应已优先选新索引（因它更窄）。
3. 逐处把查询的 `json_extract` 换成列。
4. 断言**结果集逐条等价**（步骤 1 的快照），跑全量 pytest + `doctor`。
5. 台账：新增一条 schema 迁移记录（沿用 `_LEGACY_SCHEMA_PRUNES` 的机制保证幂等与可追溯）。
6. 回滚：`DROP INDEX` + `DROP COLUMN`（实测允许），或还原备份；两者都可。

**建议顺序**（按读写权重与索引收益）：`governance_queue` → `claims` → `evidence` → `sources` →
`change_sets` → `operational_memory`（最后一表已有真列，需要先裁决「生成列 vs 已有真列」的重复拼写）。

### 提案形状

见上「冻结的基线」：生成列 + 普通索引，逐表一个提交，每表带等价性证据；`operational_memory` 放最后
（它已有真列，需要先裁决重复拼写）。

### 为什么必须整批做

只改 `governance_queue` 会留下「同一张表两种取状态方式」的混合模式，而另外 5 张表仍是旧模式；这正是
`coding.md` §1 明令禁止的形状。要么 6 张表一起定基线，要么不动。

**建议顺序**：先做 `operational_memory`（已有物化先例、读写最重、收益最大），但**基线定义**要在动手前
一次定死（哪些键升级为列、列名规则、漂移比对如何统一），然后逐表按同一模板落地——模板一致就不算混合。

---

## 二、O 系列：过度设计

审计（9-16/9-17）判定 6 项。今日复测如下，并给出各自的最小可行动作。

### O1 索引瘦身 —— **已测量，结论是「不要删」**

方法（全部只读或只在副本上）：用 pytest 插件给 `sqlite3.connect` 挂 trace 回调，跑完整套件（810 passed）抓下
**32,747 条**真实语句（其中 1,442 条可规划），再逐条对活库跑 `EXPLAIN QUERY PLAN`，统计 43 个用户索引各自被哪些
计划使用 → 得 **21 个「从未被任何被追踪计划使用」**。随后在**副本**上（post-VACUUM 备份的拷贝）把这 21 个全部
`DROP`，VACUUM 后对比大小与查询耗时。

| 查询 | 有索引 | 删除后 |
|---|---|---|
| timeline by entity | 0.01 ms | **11.74 ms** |
| timeline by action | 0.04 ms | 10.70 ms |
| timeline by sentiment | 0.01 ms | 9.56 ms |
| entity by type | 0.34 ms | **176.47 ms** |
| entity by status | 0.01 ms | 170.03 ms |
| memory by status | 9.68 ms | **632.74 ms** |
| om index by state | 14.72 ms | 117.36 ms |
| edges by source | 0.01 ms | 9.50 ms |
| nodes by domain | 0.06 ms | 31.27 ms |

**结论：不删。** 这 21 个索引省下的是 **21,413,888 B（占库 1.3%）**，代价是真实查询慢 65–500 倍。被追踪到的
计划里「没用到」是**测量方法的假象**：套件不发出这些查询形状（`timeline` 按 entity、`entities` 按 type/status、
`memory` 按 status 等），而生产/MCP 面会发出。**方法本身的教训要写下来**：用测试流量推断「索引没被使用」会把
「测试没跑这种形状」当成「没人这么查」。唯一在**结构上**可判为冗余的只有 `idx_om_type (memory_type)`（被仍在使用
的 `idx_memory_key (memory_type, …)` 前缀覆盖），而实测「memory by type」从 14.60 ms 变 22.0 ms，收益与代价同样
不值一提。**本项结束：不做减法，保留度量以避免后人重复这个「优化」。**

（`timeline_events` 确实有 10 个索引、而该表只有 10,294 行，看起来过度索引；但上表说明这些索引服务的正是按
entity/action/sentiment/title 的时间线查询，删除会让它们退回全表扫描。是否值得改成「少量复合索引覆盖多种形状」
需要另一轮设计，不属于「瘦身」。）

### O2 `.meta` 状态文件 —— 复测：14 个文件；结论是**不删**

按「谁读、谁写」逐个核查（在仓库里搜每个文件名，代码引用 = 有主）：

| 文件 | 大小 | 仓库内代码引用 | 结论 |
|---|---|---|---|
| `.watchdog_status.json` | 649 B | 7 处（含 doctor、runtime_health） | 活的 |
| `.scheduled_lint_state.json` | 87 B | `watchdog_app` | 活的 |
| `.watchdog.instance.lock` | 0 B | `watchdog_app` | 活的 |
| `purpose_vectors.json` | 189 B | `governance_store`、`purpose_contract` | 活的 |
| `storage_growth.json` | 2.2 KB | 只有 `backup_retention`（**读方在、写方已失**） | 冻结样本 |
| `community_snapshot.json` | 693 KB | `scripts/community_clustering_daemon.py` | 脚本侧活的 |
| `graph-edge-reconciliation-v1-receipt.json` | 30 KB | 无代码引用，但 `ARCH_2026-09-17.md` 引用 | **保留（见下）** |
| `auto_ingest_config.json` | 953 B | 无 | 生产者已失 |
| `.auto_ingest_controller_state.json` | 507 B | 无 | 生产者已失 |
| `.auto_ingest.execution.lock` | 0 B | 无 | 生产者已失 |
| `.heavy-task-status.json` | 910 B | 无 | 生产者已失 |
| `processing_files.json` | 3.1 KB | 无 | 生产者已失 |
| `raw_scrub_ledger.json` | 557 B | 无 | 生产者已失 |

**我原先的建议是错的，此处修正**：我曾在本文第一版里把那个 30 KB 收据当作「最明显的删除候选」。核查后发现恰好相反——

- 它**没有写方**，因此不会再变化；它是**一次不可逆删除操作的唯一记录**：里面写着权威规则（「一条边存活，当且仅当 source_id 是
  活的 entities page_key」）与删除前后的边数（`claim_graph_edges` 10293、`page_graph_edges` 10400），以及被删的边清单；
- 它被 `ARCH_2026-09-17.md` 引用，属于**归档证据**，不是垃圾状态。

而其余「无代码引用」的文件之所以失去所有者，原因在本仓库里已有记载（`ARCH_2026-09-17.md:393-398`：**本树丢失了 34 个模块**，
`storage_growth.py`、`backup_capacity`、`tool_auto_ingest` 都在其中），不是有人忘了清理。删它们会顺手删掉「那个功能曾经存在」的
最后证据，而且 `auto_ingest_config.json` 是**配置**——若仓库外仍有运行者在读它，删除是破坏性的。

**行动：不删。** 本项已完成的部分是「归属核查」本身；要做减法，前提是先确认仓库外无消费者（属另一项，需你确认是否有外部运行者）。

### O3 双份物化 —— 复测：`index.json` 14.45 MB / 7125 节点

同一份知识同时存在于：wiki Markdown（7923 文件）、`index.json`（14.45 MB，含每节点 `summary`、`aliases`、
`categories`、`weighted_edges`、`error_log`、`communities`）、SQLite 投影（`page_index_nodes`、
`page_index_edges`、`claim_graph_edges`）。今日实测该文件的构成：`nodes` 65.5%、`weighted_edges` 22.0%、
`aliases` 10.0%、`communities` 2.3%；其中 `summary` 确实每个节点至多 320 字符（中位数/均值/最大均为 320，最小 106），
合计占全文 15.8%。`claim_graph.json` 只有 133 B —— 它已经是**指针**（含
`contract`/`format_version`/`projection`/`sidecar`），说明「大文件改指针」这个方向在本项目已有先例。

**最小动作**：把 `index.json` 按 `claim_graph.json` 的方式改成指针 + SQLite 权威，先只处理 `error_log`
与 `communities` 这类**纯派生**段，保留 `nodes` 一段时间。README 关于 `index.json` 的描述也要同步收窄
（审计 O6：它并非「不再保存完整正文」，而是每节点保留约 320 字节摘要）。

### O4 四份命令注册表 —— 复测：26 / 41 / 37 / 46

argparse 子命令 26、MCP 工具 41、`tools.__all__` 37、README 的 `python cli.py` 行 46。四份清单互不校验，
所以「加了命令忘了文档」「MCP 暴露了 CLI 没有的能力」都只能靠人。

**最小动作（只读守卫，不改架构）**：加一条测试断言「argparse 子命令集合 ⊆ `tools.__all__` 可见入口」，
以及「README 提到的每个子命令都真实存在」。两者的差集就是当前漂移量。**不要**试图合并成单一注册表——
那是一次架构改动，且 MCP 与 CLI 的参数形态本来就不同。

### O5 环境开关 —— 复测：26 个不同变量

`VECTOR_LAKE_MEMORY_DIR`、`VECTOR_LAKE_MEMORY_SEARCH`、`VECTOR_LAKE_DB_PATH`、
`VECTOR_LAKE_OUTBOX_BACKLOG_BLOCKING`、`VECTOR_LAKE_IGNORE_SCHEDULED_LINT_STATE` ……

**最小动作**：给每个变量补一行**归属与默认值**登记（哪个模块读、不设时的行为），放进 CONTEXT.md 或 README
的配置章节；下一次新增开关必须同时登记。**不建议**现在做「统一配置对象」重构——收益低于风险。

### O6 摄取调度的职责表 —— 已测（只读读代码），结论：**是文档缺口，不是重复实现**

| 组件 | 触发 | 它写什么 | 与谁串行 |
|---|---|---|---|
| `watchdog_app.start_watchdog()` | 进程启动 | 编排下面三个线程 + 文件监听（Observer） | 持有 `global_task_lock`（定时维护块内） |
| ├ 文件监听线程 | 文件系统事件 | 入队（raw → 队列），不写库 | 事件级 |
| ├ `scheduled_lint_loop` | 每小时取样，当天一次 | 定时 lint、**gram 索引重建（到期时）**、WAL checkpoint、备份保留 | `global_task_lock` |
| ├ `index_worker_loop` | 5 s 轮询 | 消费 `mutation_outbox`（`process_mutation_outbox_batch`），canonical 投影 | 批级 `transaction()` |
| └ `ingest_worker.start_worker`（由 watchdog 起线程） | 5 s 轮询 | `process_jobs()` —— **从 `jobs` 表认领并处理摄取作业** | 作业租约（lease） |
| `scripts/ingest_runner.py` | 主机侧独立进程（`while True`） | **同样从 `jobs` 表认领**，但把模型调用交给外部命令（`--model-cmd`） | 作业租约（lease） |
| `scripts/ingest_runner_service.py` | 主机侧 | 看护 `ingest_runner` 进程 + 写 supervisor 状态 | 进程级 |
| `embedding_scheduler` | 显式调用（CLI/MCP） | 批量 embedding，`while True` 是**一次运行内的批次循环**，不是常驻线程 | 运行级 + RPM/TPM 租约 |

**结论**：`ingest_worker`（进程内线程）与 `ingest_runner`（主机侧进程）是**同一个作业队列的两个消费者**，
靠租约保证不冲突 —— 这是有意的设计（同一份代码在“无外部模型命令”与“用外部模型命令”两种部署下都能跑），
**但没有任何一处文档说明该在哪个部署里跑哪一个**，也没说清两者同时运行是否被支持。这不是要合并的重复实现，
而是一个**必须写下来的部署约定**。建议（未执行，需你确认部署形态）：在 README 的架构一节写明
「进程内 worker 适用于本机无外部模型命令的部署；`ingest_runner` 适用于把模型调用外置的部署；两者可同时运行
（租约安全），但同一作业只会被一方处理」。

### 明确**不**建议动的

- `node_vocabulary.py` 的零依赖叶片设计（它正是为了避免环才存在）。
- `wiki_utils.write_markdown_file` 里的编译事实/证据时间线守卫（是承重防线，不是冗余）。
- 两处**承重的**延迟导入（见 9-17 CHANGELOG，已有测试钉住）。
- 四份命令注册表合并成一份（见 O4）。

---

## 三、执行状态（2026-09-18 晚）

- **O1：已测量，结论不删**（见上；度量已保存，避免后人重复这次尝试）。
- **O2：已核查，结论不删**（其 D5 提案见下）。
- **O3：仍待决策**（依赖 D5 的「SQLite 权威」共识）。
- **O4、O5：已完成**（注册表守卫测试 + 26 个环境变量登记，见 CHANGELOG）。
- **O6：已给出职责表**（见上），剩余动作是在 README 写部署约定，需先确认部署形态。
- **D5：仍待决策**（跨批、需要冻结基线与恢复点）。
