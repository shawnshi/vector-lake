# Required runtime dependencies.
#
# CJK tokenization is layered (see vector_lake/tokenizer.py):
#   * rjieba -> jieba-rs (Rust) via the official PyO3 binding. Ships cp38-abi3
#     wheels for Windows/macOS/manylinux/musllinux, so no compiler is needed on
#     the supported platforms. Measured 7-15x faster here.
#   * jieba  -> pure-Python fallback, always installable, and the only backend
#     exposing add_word() for custom dictionary terms.
#
# Community detection uses Leiden (leidenalg on igraph), which replaced
# python-louvain/Louvain; see scripts/community_clustering_daemon.py.
#
# mistune is pinned to >=3.0.1, not >=3.0: create_markdown(renderer='ast') raises
# TypeError on 3.0.0 and only works from 3.0.1 onwards (verified by running both).

filelock>=3.15
networkx>=3.2
python-dotenv>=1.0
igraph>=0.11.0
leidenalg>=0.10.0
PyYAML>=6.0.1
watchdog>=4.0
google-genai>=2.0.0
# fastmcp 4.x is the server framework: mcp_server.py imports `fastmcp.FastMCP`.
# The MCP Python SDK (`mcp`) is no longer a direct dependency -- fastmcp-slim
# resolves it (2.2.0) transitively -- and the 2.x `mcp.server.MCPServer` name is
# not part of this contract. A floor below 4.0 loses the synchronous tool
# registry (`_tool_manager` is gone), so the floor is load-bearing.
fastmcp>=4.0.0
sqlite-vec>=0.1.3
rjieba>=0.2.1
jieba>=0.42.1
mistune>=3.0.1
bm25s>=0.2.0
