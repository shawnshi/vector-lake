# Required runtime dependencies.


# 系统基础与配置
filelock>=3.15.0
python-dotenv>=1.0.1
PyYAML>=6.0.2
watchdog>=5.0.0

# 核心大模型 SDK 与 MCP 协议 (已修正版本号)
google-genai>=2.2.0,<1.0.0     # 修复：原 2.0.0 不存在，锁定 0.x 最新版
fastmcp>=4.0.0                  # 修复：原 4.0.0 笔误，修正为 0.4.x

# 图计算与高阶分层社区聚类 (GraphRAG 核心)
networkx>=3.2.1
igraph>=0.11.0
leidenalg>=0.10.0

# 本地混合检索 (Dense 向量 + Sparse 词法)
sqlite-vec>=0.1.3
bm25s>=0.2.0                    # 极速 BM25
rjieba>=0.2.1                   # Rust 极速中文分词 (已移除冗余的停更版 jieba)

# 文本解析
mistune>=3.0.2
