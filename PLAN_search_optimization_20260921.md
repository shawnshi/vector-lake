# search 修改优化方案（2026-09-21）

对象：`vector_lake/tool_search.py` 的检索路径，以及它的评测基础设施。
上游：`AUDIT_search_pipeline_20260920.md`（审计 A1–A6 / D1–D3）、`PLAN_search_A_remediation_20260920.md`（A 部分方案）、
`benchmarks/search_eval_decisions_round2.md`（第二批预注册）。
状态：**方案，不含实施**。凡是"已实测"的数字都标了出处；凡是还没测的都写成待测，不预支结论。

---

## 0. 先纠正一条（免得继续按错误前提做方案）

上一轮我准备把"向量只覆盖 2602 / 7175 页，六成页面向量检索永远看不见"写成本方案的第一根支柱。
**实测推翻了它**（`wiki/.meta/vector_lake.db`，只读）：

| | |
|---|---|
| `page_index_nodes` | 7175 |
| `vec_embeddings` | **7175（100%）** |
| 按前缀 | Concept 3983/3983、Source 1787/1787、Product 486/486、Vendor 315/315、Person 250/250、Event 140/140…全部 100% |

审计报告里的 2602 是 2026-09-20 的状态；你正在落地的兜底补向量（`VECTOR_LAKE_CATCHUP_EMBEDDING_BATCH`）已经把它补满，库文件 18:38 仍在写入。
**结论：向量盲区不再是瓶颈，不进方案。** 审计里的取证纪律在这里再验一次：昨天的数不能当今天的因。

---

## 1. 现在的证据基础

**融合本身**（第一批，116 查询，4 个标注集 × 3 位判定者，跨族 kappa 0.70–0.77）：
rrf 相对 sum 在 16/16 个"指标 × 标注集"组合上点估计都为正，但 **nDCG@5 的 95% CI 一律含 0**，且全部点估计 < +0.05；
增益稳定出现的只有 recall@5。规则 NOT MET，默认保持 `sum`。

**第二批**（333 条新查询，三位判定者，进行中）：主判定者 p 已交齐 333/333 且父侧校验零问题：

| | |
|---|---|
| 相关页次 | 89（≈0.27/查询） |
| **零相关查询** | **268（80%）** |
| 有效确认集（至少一页相关） | **65 / 333** |

**这批数字怎么读，是整份方案的分水岭。**
候选池 = 两路各 top-5 的并集（实际 6–17 条/查询）。所以"80% 的查询池里没有主体对得上的页"不是语料问题（向量已 100% 覆盖），而是
**一个 top-5 召回的实测值**：要么答案在第 6 名往后，要么排序根本没把它抬上来。

---

## 2. 方案：按"谁能改变结论"排序

### P0 先定位病因：答案在第几名？（只测量，不动产品代码）

对 p 判为"池内零相关"的那 268 条查询，把候选深度从 top-5 扩到 top-20，重新盲判一次，得到
**"首个相关页的名次分布"**。一次测量同时回答两个互斥假设：

* 相关页多落在 **6–20 名** → **深度问题**：候选深度（`limit=top_k*5`）与池规模（`max(40, top_k*3)`）是杠杆，改动点在取数而不是打分；
* 20 名内也找不到 → **排序/表征问题**：才轮到 A1 的量纲、A2 的扩展配额、`_expand_query_locally` 的查询整形。

* 成本：约 20 批判定（沿用 v3.1 的 20 查询/批、fresh 上下文、父侧校验）；
* 收益：把 P1 的方向钉死，避免在"融合"这个已被证明只有 recall@5 有增益的地方继续投算力；
* 复现方式与第二批一致（同一盲化格式、同一判据、同一批粒度），因此两者可比。

### P1 排序语义（改动点已存在，且都带开关）

依赖 P0 给出的方向，逐项单独 A/B，**禁止一次翻多个开关**：

1. `VECTOR_LAKE_FUSION`（`sum|rrf`）：等第二批结论；若 NOT MET 则维持 `sum`（预注册 §5）。
2. `VECTOR_LAKE_EXPANSION_QUOTA`（默认 0 = 今天的行为）：**建了但从未在任何标注集上度量过**。A2 已证明扩展候选在融合单位下 ≤ ~0.45，只能占剩余槽位，且 5 个抽样查询里有 3 个槽位为 0 —— 也就是说这条路径的收益上限由池规模决定，属于 P0 的因变量。
3. A1 的量纲：`fts_score = -bm25` 无界（实测区间 5.1–47.6）而 `vec_score = sim²·15` 硬顶 15。同一套代码在不同查询下把决定权交给不同一路（`AI 医院` 向量主导，前四例 FTS 主导）。这是"混排没有可比语义"的根，改它要么走 RRF（已实现），要么对两路各自归一——但**先看 P0 的结果再决定值不值得动**。

### P2 契约与精度加固（不动排序，可与 P0/P1 并行）

1. ~~**A4 键契约**~~ **✓ 已完成 2026-09-22**：`vec_embeddings.entity_id` → `page_key`。vec0 拒绝 `RENAME/ADD COLUMN`，且任何 RENAME 都会打碎影子表（两种写法都在一次性小库实测复现）→ 走“暂存表 → DROP → 以最终名字重建 → 搬回”的单事务路径；7175 行、键集合、影子表全部完好，自身向量的 `MATCH` 首条 `distance=0.0`，全量测试 1378 passed。恢复点在 `C:/Users/shich/backups/vector-lake/`。守卫测试升级为“不得跨命名空间连接 + 旧列名不得回来”。
2. **D3 两个对外数字**：`memory_warning_count` 结构性封顶 6（`[:6]` 先截断再计数）；`omitted_count` 只在字符截断时置值，实测 24 条记忆时 `Evidence Pointers` 只出 12 条却报 0。都是喂给 Agent 的机器可读字段，"报了但不对"。

### P3 评测基础设施（今天暴露出来的）

1. **"有效确认集"升级为一等指标**：harness 那道 300 的门数的是"有标签的查询"（333 全数通过），与"有页可找的查询"（今天只有 65）不是一回事。数字必须并列呈现，且不得用前者冒充后者。
2. **判定协议已固化并入库**：fresh 上下文（fork 会让 child 继承父会话的假设并去执行父代理的计划）、批 ≤20 查询、父侧统一校验、键前缀纪律、单写者。
3. **池深度与判定者配置写进 registration**：P0 的 top-20 与第二批的 top-5 是两个不同仪器，必须在做之前登记，不能事后混用。

---

## 3. 明确不做（都有证据支持）

* 不因为 recall@5 显著就把主指标改成 recall@5 —— 事后挑指标；
* 不在主判定者弃权率高的前提下换主判定者或放宽判据 —— 同样的事后挑选；弃权率本身作为结果报告（它是对 top-5 召回的直接测量）；
* 不把向量覆盖率当瓶颈（已 100%）；
* 不重跑第一批 116 条（已被三位判定者看过，无法再作确认集）；
* 不在 P0 出结果前同时翻多个开关。

---

## 4. 验收与门

每项改动：harness 回放 + 四组对比（`{primary, consensus} × {sum, rrf}`）+ 次要指标不回退 + 报告有效确认集；
翻默认沿用预注册 §5 的动作（主判定者与最严共识集**两处都过**才翻，且单独一次提交、开关保留）。
P2 的 A4 迁移按有状态变更走：恢复点、回滚步骤、迁移后关键验收（`vec_embeddings` 行数、`MATCH` 查询仍返回、键命中率 100%）。

---

## 5. 需要你定的三处

1. **P0 是否现在就跑**？约 20 批判定（268 条查询、top-20 深度），产出"答案在第几名"的分布。这是目前唯一能决定后续排序改动方向的测量。
2. **A4 迁移是否给窗口**？需要独占写 DB（你的兜底补向量正在写同一个库），且要回滚点。
3. **第二批结论出炉后**：若按预注册 §5 判为 NOT MET，是照"停止评估、默认保持 `sum`"执行，还是把 P0/P1 作为**新的、另行预注册**的问题继续？（预注册写的是任一处不过即停止追加批次，除非有新理由；"top-5 召回只有 20%"正是那个新理由，但应由你确认。）

---

# P0 结果（2026-09-22，实测）

**做法**：把那 268 条"第二批 top-5 池内零相关"的查询，用**同一套仪器**（同一个 `run()`、同一份冻结向量快照）在 **top-20** 深度重取候选（`sum` ∪ `rrf`，6999 候选），再由**同一位主判定者**盲判 72 批（268/268 条，行数全对、零池外键）。然后读"首个相关页的名次"。

## 名次分布

| 首个相关页名次 | 查询数 | 占比 |
|---|---|---|
| 1–5 | 45 | 16.8% |
| 6–10 | 4 | 1.5% |
| 11–20 | 6 | 2.2% |
| **20 名内无** | **213** | **79.5%** |

两路各自在 20 名内至少命中一条相关页：`sum` 48（17.9%）、`rrf` 55（20.5%）。

## 拆开 45 条"1–5 名"：三种成因，后果完全不同

| 成因 | 查询数 | 占比 | 含义 |
|---|---|---|---|
| 在 1–5 名，且该页**本来就在 top-5 池里** | **9** | 3.4% | 同一页、同一查询，第二批判"不相关"、P0 判"相关" → **判定者自相矛盾** |
| 在 1–5 名，但**不在 top-5 池里** | **36** | 13.4% | 排序本来把它放进前 5，**结果集构造把它丢了** |
| 在 6–20 名 | **10** | 3.7% | 真正的"深度不够" |

那 36 条的页类分布（44 个页次）：`Concept_` 20、`Source_` 12、`Standard_` 5、`Vendor_` 3、`Policy_` 2 —— **不是只有 Source 类**。

### 由此定位到一个具体缺陷：top-k 的结果集不嵌套

三个上限都随 `top_k` 缩放：`pool = max(40, top_k*3)`、`max_sources_pool = int(pool*0.6)`、
`max_sources_final = int(top_k*0.6)`。于是 **k 小时的返回集不是 k 大时的前缀**：一个页在 k=20 下排进前 5，在 k=5 下可能**根本不被返回**。D2 早已记下这个启发式的非单调（`top_k=1` → 允许 0 个 source 页），P0 给出了它的**实际规模：占这批弃权的 13.4%**。

评测看到的是"没找到"，实际是"被上限挡掉了"。这一条修起来便宜、且不需要动任何打分逻辑。

## 这对原方案的改写

| 原排序 | 现排序（按实测收益） |
|---|---|
| ~~P1 先改融合/排序语义~~ | **P1a：让 top-k 结果集嵌套**（去掉/修好随 k 缩放的上限）。13.4% 的弃权落在这里，且 D2 已记录其非单调性 |
| — | **P1b：判定者自洽性**。3.4% 的查询上，同一页被同一判定者在两轮里判成相反结论；第二批的结论正建立在这批标签上，应显式测一次重测一致性 |
| — | **P0.5：判据可满足性**（79.5% 落在 top-20 之外）。粗探显示 261/264 的查询存在"标题就含查询词、仍被判非主体"的候选 —— 该探针在中英混排上偏宽松，**只能作提示**；真正的定论要换判据重判，那是换仪器，须另行预注册 |
| P2/P3 | 不变 |

**P0 对原问题的回答（写清楚）**：深度是**小杠杆**（3.7%）；排序早在前 5 就把答案放好了，是**取数／池构造**丢掉的（13.4%）；**主要矛盾是判据与语料的匹配**（79.5%），而那不是靠改融合能挪动的。

## P0 那条"13.4%"的更正（2026-09-22，验证 P1a 时发现）

上面把 36 条（13.4%）归因于"结果集构造/上限把它丢了"。**这个归因被后来的测量推翻了**：冻结的 v3.1 池与今天的 k=5 返回集已经不是同一回事 ——

| | |
|---|---|
| 今天 k=5 返回、但不在冻结池里的页键 | **664** |
| 在冻结池里、但今天 k=5 不返回的页键 | **1348** |
| 两个集合不一致的查询数 | **330 / 333** |
| 那 664 个新页键的来路 | `vec` 640 + `both` 8 + `fts` 10 + `ppr` 6 → **97.6% 经向量这一路** |

即：**池是在向量兜底补齐之前冻结的（旧索引 2602 向量），而计分用的 run 是补齐之后跑的（7175 向量）**。向量补齐（你自己的 `VECTOR_LAKE_CATCHUP_EMBEDDING_BATCH` 那一支）落在两者之间。

因此：
* "36 条被上限挡掉"里，有相当一部分其实是**索引变化**，不是上限。真正能站住的证据是 P1a 自身的验收（见下），不是这个归因；
* 第二批结论的**相对比较仍然成立**（sum 与 rrf 用的是同一批 run、同一批标签，两边被同样对待），但它的**绝对水平（nDCG≈0.12）与"80% 查询池内无相关页"这两个数，是旧索引的属性**，不能当作当前系统的性质；
* 结构性缺陷：`search_replay.py` 冻结了**查询向量**，却没冻结**语料**（页面索引 + 语料向量）→ 标签与计分可以各自漂移，而且已经漂了。

## P1a 验收（2026-09-22，实测）

| 检查 | 结果 |
|---|---|
| 默认 `top_k=5` 行为是否改变 | **未改变**：四个「标注集 × 融合」回放的指标与**返回页键逐条相同**（333/333 查询，0 处不同） |
| `top5 == top40[:5]` 且 `top20 == top40[:20]` | **333/333** |
| 集合嵌套 `top5 ⊆ top20 ⊆ top40` | **333/333** |
| 代价：`top20` 窗口未满（来源类上限所致） | **104/333（31%）** |

最后一行是新引入的真实代价：来源类上限从 `max(1, int(top_k*0.6))` 改成绝对值 `3` 之后，池子来源类偏多时，`top_k=20` 可能只返回不到 20 条。要不要把它换成"固定的排序惩罚"（不再截断窗口）是一个**独立、可单独度量**的下一步。
