---
name: gc
description: 'Automatically prune isolated or orphaned entities from the knowledge graph.'
---
Please use the `gc_vector_lake` MCP tool from the `vector-lake-mcp` server. 
You can pass `days` to specify the age threshold, and `dry_run=True` to preview what would be deleted without making changes.
