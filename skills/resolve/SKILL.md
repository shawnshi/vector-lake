---
name: resolve
description: Resolve a pending Vector Lake governance item.
---
Please use the `resolve_governance_item` MCP tool from the `vector-lake-mcp` server to resolve an item.
You must provide the `item_id` and the `resolution` ('skip', 'create', 'merge', or 'acknowledge').
