description = "Launch the real-time ingest compiler as a background service."
prompt = '''
Please use the run_command tool to execute "python watchdog_sync.py" as a background service, with WaitMsBeforeAsync=500. Do not substitute one-off sync commands for this daemon. Inform the user that the background ingest compiler service has started.
'''
