description = "Launch the real-time LLM-Wiki ingest compiler. It listens to the MEMORY/raw directory and incrementally compiles new sources into Markdown pages. Runs as a background service."
prompt = '''
Please use the run_command tool to execute "python watchdog_sync.py". Make sure to send it to the background by setting WaitMsBeforeAsync=500. Inform the user that the background ingest compiler service has started.
'''