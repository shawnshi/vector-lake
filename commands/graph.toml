description = "Generate the interactive 3D HTML topology dashboard without running a full governance audit."
prompt = """
Please call the `visualize_vector_lake` MCP tool from the `vector-lake-mcp` server to generate the topology graph.
"""
