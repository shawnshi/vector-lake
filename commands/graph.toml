description = "Generate the interactive 3D HTML topology dashboard without running a full governance audit."
prompt = """
Ask the user to approve an absolute output directory under their `.codex` or `.gemini` scratch area, then call `visualize_vector_lake` with that exact `output_dir`. Report completion only after the returned file resolves inside the approved directory and exists.
"""
