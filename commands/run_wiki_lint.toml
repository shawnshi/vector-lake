description = "Trigger a comprehensive 10-point health check of the Wiki Markdown network (V7.0)."
prompt = """
Please use the `lint_vector_lake` tool with `auto_fix=True` to trigger a full system health check. This scans frontmatter completeness, naming compliance, type/status legality, category vocabulary, duplicate IDs, alias conflicts, broken links, orphan pages, file name similarities, and knowledge decay (>60 days sprouting). Read out the generated report to the user.
"""