description = "Query the LLM-Wiki to synthesize insights and auto-generate new Wiki pages (Query-to-Page)."
prompt = """
Please use the `query_logic_lake` tool and pass the query text "{{args}}". This will engage the Vector Lake engine to compute a Tacit Subgraph Context and deploy a headless Agentic compiler to evaluate and write the result natively. Inform the user once it completes.
"""